services:
  - type: web
    name: mg13-dashboard
    runtime: node
    plan: starter
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /opt/render/project/src/storage
    disk:
      name: mg13-dashboard-storage
      mountPath: /opt/render/project/src/storage
      sizeGB: 1
